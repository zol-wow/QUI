-- Profile already at current schema. Migrations should be a no-op.
QUI_DB = {
    profileKeys = { ["TestChar - TestRealm"] = "Default" },
    profiles = {
        Default = {
            _schemaVersion = 35,
            cdm = { engine = "owned" },
        },
    },
}
QUIDB = {}
